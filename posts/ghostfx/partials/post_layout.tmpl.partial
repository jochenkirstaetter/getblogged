{{! The tag above means: insert everything in this file
into the {body} of the default.hbs template }}

<header class="site-header outer">
    <div class="inner">
        {{>partials/site-nav}}
    </div>
</header>

{{! Everything inside the #post tags pulls data from the post }}


<main id="site-main" class="site-main outer">
    <div class="inner">

        <article class="post-full {{postClass}} {{^image}}{{^featureImage}}no-image{{/featureImage}}{{/image}}">

            {{#image}}
            <figure class="post-full-image">
                <img
                    src="{{_rel}}{{image}}"
                    alt="{{title}}{{#imageAttribution}} - {{.}}{{/imageAttribution}}"
                    width="1600"
                    height="900"
                    fetchpriority="high"
                    decoding="async"
                />
            </figure>
            {{/image}}
            {{^image}}
            {{#featureImage}}
            <figure class="post-full-image">
                <img
                    src="{{_rel}}{{featureImage}}"
                    alt="{{title}}{{#imageAttribution}} - {{.}}{{/imageAttribution}}"
                    width="1600"
                    height="900"
                    fetchpriority="high"
                    decoding="async"
                />
            </figure>
            {{/featureImage}}
            {{/image}}

            <header class="post-full-header">
                <h1 class="post-full-title">{{title}}</h1>
                {{>partials/markdown-actions}}
            </header>

            <section class="post-full-content">
                <section class="post-full-meta">
                    <time class="post-full-meta-date" datetime="{{#publishedAt}}{{.}}{{/publishedAt}}{{^publishedAt}}{{date}}{{/publishedAt}}">{{#formattedDate}}{{.}}{{/formattedDate}}{{^formattedDate}}{{#date}}{{.}}{{/date}}{{^date}}{{#publishedAt}}{{.}}{{/publishedAt}}{{/date}}{{/formattedDate}}</time>
                    {{#tags.0}}
                        <span class="date-divider">-</span>
                        <span class="post-full-meta-tags">Filed under {{#tags}}<a class="post-card-tags" href="{{_rel}}tags/{{.}}.html">{{.}}</a>{{/tags}}</span>
                    {{/tags.0}}
                </section>
                <script>
                (function() {
                    var el = document.currentScript ? document.currentScript.previousElementSibling.querySelector('.post-full-meta-date') : document.querySelector('.post-full-meta-date');
                    if (el) {
                        var raw = el.getAttribute('datetime') || el.textContent.trim();
                        var m = raw.match(/^(\d{4})-(\d{1,2})-(\d{1,2})/);
                        if (m) {
                            var months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
                            var y = parseInt(m[1], 10);
                            var mo = parseInt(m[2], 10) - 1;
                            var d = parseInt(m[3], 10);
                            if (mo >= 0 && mo < 12) { el.textContent = months[mo] + ' ' + d + ', ' + y; }
                        }
                    }
                })();
                </script>
                <div class="post-content">
                    {{{conceptual}}}
                </div>
            </section>

            {{! Email subscribe form at the bottom of the page }}
            {{#@labs.subscribers}}
            <section class="subscribe-form">
                <h3 class="subscribe-form-title">Subscribe to {{_appTitle}}</h3>
                <p>Get the latest posts delivered right to your inbox</p>
                {{subscribe_form placeholder="youremail@example.com"}}
            </section>
            {{/@labs.subscribers}}

            <footer class="post-full-footer">

                {{! There are two options for how we display the byline/author-info.
                If the post has more than one author, we load a specific template
                from includes/byline-multiple.hbs, otherwise, we just use the
                default byline. }}

                {{#hasMultipleAuthors}}
                    {{>partials/byline-multiple}}
                {{else}}
                    {{>partials/byline-single}}
                {{/hasMultipleAuthors}}

            </footer>

	    {{>partials/ads-banner}}

            {{#_disqusSiteId}}
            <section class="post-full-comments">
                <div id="disqus_thread"></div>
                <script>
                    (function() {
                        var loaded = false;
                        function loadDisqusEmbed() {
                            if (loaded) return;
                            loaded = true;
                            window.disqus_config = function () {
                                this.page.url = '{{_rel}}{{slug}}.html';
                                this.page.identifier = '9271ed1c97_id{{uid}}';
                            };
                            var d = document, s = d.createElement('script');
                            s.src = 'https://{{_disqusSiteId}}.disqus.com/embed.js';
                            s.setAttribute('data-timestamp', +new Date());
                            (d.head || d.body).appendChild(s);
                        }
                        var target = document.getElementById('disqus_thread');
                        if ('IntersectionObserver' in window && target) {
                            var observer = new IntersectionObserver(function(entries) {
                                if (entries[0].isIntersecting) {
                                    loadDisqusEmbed();
                                    observer.disconnect();
                                }
                            }, { rootMargin: '300px' });
                            observer.observe(target);
                        } else {
                            window.addEventListener('load', function() {
                                if ('requestIdleCallback' in window) {
                                    requestIdleCallback(loadDisqusEmbed, { timeout: 4000 });
                                } else {
                                    setTimeout(loadDisqusEmbed, 3000);
                                }
                            });
                        }
                    })();
                </script>
            </section>
            {{/_disqusSiteId}}

        </article>

        {{^_disableContribution}}
        <div class="contribution d-print-none">
        {{#sourceurl}}
        <a href="{{sourceurl}}" class="edit-link">{{__global.improveThisDoc}}</a>
        {{/sourceurl}}
        {{^sourceurl}}{{#docurl}}
        <a href="{{docurl}}" class="edit-link">{{__global.improveThisDoc}}</a>
        {{/docurl}}{{/sourceurl}}
        </div>
        {{/_disableContribution}}

        {{^_disableAffix}}
        <div class="affix">
            <nav id="affix"></nav>
        </div>
        {{/_disableAffix}}

    </div>
</main>

{{! Links to Previous/Next posts }}
{{^_disableNextArticle}}
<aside class="read-next outer">
    <div class="inner">
        <div class="read-next-feed">
            {{#primary_tag}}
            
                {{#related_posts}}
                <article class="read-next-card"
                    {{#../primary_tag.image}}
                        style="background-image: url({{_rel}}{{../primary_tag.image}})"
                    {{else}}
                        {{#@blog.cover_image}}
                            style="background-image: url({{_rel}}{{@blog.cover_image}})"{{/@blog.cover_image}}
                    {{/../primary_tag.image}}
                >
                    <header class="read-next-card-header">
                        <small class="read-next-card-header-sitetitle">&mdash; {{_appTitle}} &mdash;</small>
                        {{#../primary_tag}}
                        <h3 class="read-next-card-header-title"><a href="{{_rel}}{{slug}}.html">{{title}}</a></h3>
                        {{/../primary_tag}}
                    </header>
                    <div class="read-next-divider">{{>partials/icons/infinity}}</div>
                    <div class="read-next-card-content">
                        <ul>
                            {{#related_posts}}
                            <li><a href="{{_rel}}{{slug}}.html">{{title}}</a></li>
                            {{/related_posts}}
                        </ul>
                    </div>
                    <footer class="read-next-card-footer">
                        <a href="{{#../primary_tag}}{{_rel}}{{slug}}.html{{/../primary_tag}}">{{plural meta.pagination.total empty='No posts' singular='% post' plural='See all % posts'}} →</a>
                    </footer>
                </article>
                {{/related_posts}}
            
            {{/primary_tag}}

            {{! If there's a next post, display it using the same markup included from - partials/post-card.hbs }}
            {{#next_post}}
                {{>partials/post-card}}
            {{/next_post}}

            {{! If there's a previous post, display it using the same markup included from - partials/post-card.hbs }}
            {{#prev_post}}
                {{>partials/post-card}}
            {{/prev_post}}

        </div>
    </div>
</aside>
{{/_disableNextArticle}}

{{! The #contentFor helper here will send everything inside it up to the matching #block helper found in default.hbs }}


